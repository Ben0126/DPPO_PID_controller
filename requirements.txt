# Deep Reinforcement Learning
stable-baselines3>=2.0.0
gymnasium>=0.28.0

# Deep Learning (CUDA 12.8 / 12.9 GPU version)
# --extra-index-url tells pip to also search PyTorch's CUDA wheel index,
# so torch/torchvision/torchaudio will be installed with cu128 support.
# Compatible with CUDA 12.8 and 12.9 (RTX 30/40 series).
# If you need CPU-only, replace the URL with https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Numerical Computing
numpy>=1.24.0
scipy>=1.10.0

# Visualization and Logging
matplotlib>=3.7.0
tensorboard>=2.12.0

# Data Storage (for expert demonstrations)
h5py>=3.8.0

# Image Processing (for synthetic FPV rendering)
opencv-python>=4.7.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
rich>=13.0.0
